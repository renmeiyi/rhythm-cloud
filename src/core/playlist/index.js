import { PlaylistManager, PLAY_MODE } from './playlistManager.js';
import './types.js'; // 引入 types 文件（主要用于类型提示）

export { PlaylistManager, PLAY_MODE };


// import { PlaylistManager, PLAY_MODE } from './playlist/index.js';

// const playlist = new PlaylistManager();
// playlist.add({ id: 1, title: 'Song A', url: 'a.mp3' });
// playlist.setMode(PLAY_MODE.RANDOM);
